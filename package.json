{
  "name": "fivem-attributes",
  "description": "Player attributes for FiveM.",
  "author": "arlofonseca",
  "license": "MIT",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "pull": "npx prisma db pull",
    "generate": "npx prisma generate",
    "build": "node build.js",
    "watch": "node build.js --watch",
    "ci": "pnpm run pull && pnpm run generate && pnpm run build"
  },
  "dependencies": {
    "@overextended/ox_core": "^1.1.3",
    "@overextended/ox_lib": "latest",
    "@prisma/client": "^5.21.1"
  },
  "devDependencies": {
    "@citizenfx/server": "latest",
    "@types/node": "^22.7.5",
    "esbuild": "^0.24.0",
    "esbuild-plugin-fileloc": "^0.0.6",
    "prisma": "^5.21.1",
    "typescript": "^5.6.3"
  }
}
